# Phase 1.5 smoke-test: a long-running container that prints the embedded
# git SHA + build time every 5s, alongside Watchtower watching for new
# teuton:latest pushes on Docker Hub.
#
# Start:
#   doppler run --project arbos --config dev -- bash -lc \
#     'echo $DOCKER_PAT | docker login -u $DOCKER_USER --password-stdin && \
#      DOCKER_USER=$DOCKER_USER docker compose -f docker/compose.wt-test.yml up -d'
#
# Tail:
#   docker logs -f teuton-wt-test
#
# Trigger an update:
#   1) touch any file (e.g. docker/entrypoint.sh)
#   2) ./scripts/build_push.sh        (rebuilds + repushes :latest)
#   3) within ~30-60s the Watchtower service will pull the new image and
#      recreate teuton-wt-test; the printed git_sha will change.
name: teuton-wt-test

services:
  teuton-wt-test:
    image: ${DOCKER_USER}/teuton:latest
    container_name: teuton-wt-test
    restart: always
    pull_policy: always
    entrypoint: ["/bin/bash", "-lc"]
    command:
      - |
        while true; do
          echo "[wt-test] $$(date -u +%FT%TZ) git_sha=$${TEUTON_GIT_SHA:-unknown} build=$${TEUTON_BUILD_TIME:-unknown}"
          sleep 5
        done
    labels:
      - com.centurylinklabs.watchtower.enable=true

  watchtower:
    image: containrrr/watchtower:latest
    container_name: teuton-wt-test-watchtower
    restart: always
    environment:
      WATCHTOWER_LABEL_ENABLE: "true"
      WATCHTOWER_CLEANUP: "true"
      WATCHTOWER_INCLUDE_RESTARTING: "true"
      WATCHTOWER_DEBUG: "true"
      WATCHTOWER_POLL_INTERVAL: "30"
      DOCKER_API_VERSION: "1.48"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${HOME}/.docker/config.json:/config.json:ro
    command:
      - --label-enable
      - --interval=30
      - --cleanup
